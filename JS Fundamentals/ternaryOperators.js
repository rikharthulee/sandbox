const agreeOrDisagreeWithTernary = (str1, str2) =>
  str1 === str2 ? "You agree" : "You disagree";

const agreeOrDisagree = (str1, str2) => {
  if (str1 === str2) {
    return "You agree";
  } else {
    return "You disagree";
  }
};
